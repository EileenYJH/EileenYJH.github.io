---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import RoboticArm from '../components/RoboticArm.astro';
import ProjectCard from '../components/ProjectCard.astro';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
const fmt = new Intl.DateTimeFormat('en', { day: 'numeric', month: 'short', year: 'numeric' });
---
<BaseLayout title="Eileen Yeoh — engineer of interactive systems">
  <section class="hero wrap">
    <div class="hero-copy">
      <p class="eyebrow" data-reveal>Electrical &amp; electronics engineering</p>
      <h1 data-split>I build systems where <em>hardware</em> meets <em>intelligence</em>.</h1>
    </div>
    <RoboticArm />
    <div class="hero-scroll" aria-hidden="true"><span></span></div>
  </section>

  <section class="hero-intro wrap">
    <p class="muted hero-sub" data-reveal>
      Sensor networks, embedded firmware, and the theory behind them —
      explained visually, built to be played with.
    </p>
    <div class="hero-actions" data-reveal data-reveal-delay="0.12">
      <a class="btn" href="/projects/aria">Explore ARIA</a>
      <a class="btn btn--ghost" href="/about">About me</a>
    </div>
    <dl class="spec-strip" data-reveal data-reveal-delay="0.2">
      <div><dt>Systems built</dt><dd>03</dd></div>
      <div><dt>ESP32 mesh nodes</dt><dd>06</dd></div>
      <div><dt>Batch time cut</dt><dd>−98%</dd></div>
      <div><dt>Bedrock agents</dt><dd>05</dd></div>
    </dl>
  </section>

  <section class="section wrap">
    <p class="eyebrow" data-reveal>Featured work</p>
    <h2 data-split>Projects you can <em>play</em> with</h2>
    <div class="proj-grid">
      {projects.map((p, i) => (
        <ProjectCard
          title={p.data.title}
          summary={p.data.summary}
          tags={p.data.tags}
          href={p.data.href ?? '/projects'}
          index={i}
        />
      ))}
    </div>
  </section>

  <section class="section wrap about-teaser">
    <p class="eyebrow" data-reveal>About</p>
    <h2 data-split>Engineering is a story worth <em>telling well</em>.</h2>
    <p class="muted" data-reveal>
      I'm an electrical &amp; electronics engineering student who believes a
      project isn't finished until someone else can understand it. This site is
      where my circuits, firmware, and systems get explained the way I wish
      textbooks did.
    </p>
    <a class="btn btn--ghost" href="/about" data-reveal>More about me</a>
  </section>

  <section class="section wrap">
    <p class="eyebrow" data-reveal>Writing</p>
    <h2 data-split>Latest from the blog</h2>
    <div class="post-list">
      {posts.map((post) => (
        <a class="post-row" href={`/blog/${post.id}`} data-reveal>
          <span class="muted post-date">{fmt.format(post.data.date)}</span>
          <span class="post-title">{post.data.title}</span>
          <span class="post-arrow">→</span>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .hero.wrap { width: min(1280px, 100% - 3rem); }
  .hero {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    align-items: center;
    gap: 2rem;
    height: calc(100svh - 58px);
    min-height: 430px;
    padding-block: 0.5rem 3rem;
  }
  .hero::before {
    content: '';
    position: absolute;
    right: -8%;
    top: 8%;
    width: 60%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(29, 78, 216, 0.1), transparent 65%);
    pointer-events: none;
  }
  .hero h1 { font-size: clamp(2.5rem, 5vw, 4.5rem); max-width: 15ch; }
  .hero-scroll {
    position: absolute;
    bottom: 1.1rem;
    left: 50%;
    transform: translateX(-50%);
    width: 2px;
    height: 42px;
    border-radius: 2px;
    background: var(--line);
    overflow: hidden;
  }
  .hero-scroll span {
    display: block;
    width: 100%;
    height: 12px;
    border-radius: 2px;
    background: var(--accent);
    animation: scroll-cue 1.9s ease-in-out infinite;
  }
  @keyframes scroll-cue {
    0% { transform: translateY(-12px); }
    100% { transform: translateY(42px); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-scroll span { animation: none; }
  }
  .hero-intro { padding-block: 4rem 0; }
  .hero-sub { font-size: 1.2rem; max-width: 46ch; }
  .hero-actions { display: flex; gap: 1rem; margin-top: 1.8rem; flex-wrap: wrap; }
  .spec-strip {
    display: flex;
    flex-wrap: wrap;
    gap: 1.4rem 3.5rem;
    margin-top: 3.2rem;
    padding-top: 1.6rem;
    border-top: 1px solid var(--line);
  }
  .spec-strip dt {
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--muted);
  }
  .spec-strip dd {
    margin: 0.1rem 0 0;
    font-family: var(--font-display);
    font-size: clamp(1.9rem, 3.5vw, 2.6rem);
    font-weight: 560;
    color: var(--accent);
    font-variant-numeric: tabular-nums;
    line-height: 1.1;
  }
  .proj-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-top: 2.5rem;
  }
  .proj-grid > :global(.proj-card:first-child) { grid-column: 1 / -1; }
  @media (max-width: 700px) {
    .proj-grid { grid-template-columns: 1fr; }
  }
  .about-teaser h2 { max-width: 18ch; }
  .about-teaser .btn--ghost { margin-top: 1.8rem; }
  .post-list { margin-top: 2.5rem; border-top: 1px solid var(--line); }
  .post-row {
    display: grid;
    grid-template-columns: 9rem 1fr auto;
    gap: 1.5rem;
    align-items: baseline;
    padding: 1.4rem 0.5rem;
    border-bottom: 1px solid var(--line);
    text-decoration: none;
    transition: background 0.2s ease;
  }
  .post-row:hover { background: var(--paper-raised); padding-left: 1rem; }
  .post-row { transition: background 0.25s ease, padding-left 0.3s cubic-bezier(0.22, 1, 0.36, 1); }
  .post-title { font-family: var(--font-display); font-size: 1.25rem; }
  .post-arrow { color: var(--accent); transition: transform 0.3s cubic-bezier(0.22, 1, 0.36, 1); }
  .post-row:hover .post-arrow { transform: translateX(5px); }
  .post-date { font-size: 0.85rem; }
  @media (max-width: 800px) {
    .hero { grid-template-columns: 1fr; text-align: center; align-content: center; gap: 1rem; }
    .hero h1 { margin-inline: auto; }
    .hero-intro { text-align: center; }
    .hero-sub { margin-inline: auto; }
    .hero-actions { justify-content: center; }
    .post-row { grid-template-columns: 1fr auto; }
    .post-date { grid-column: 1 / -1; }
  }
</style>
